import {StyleSheet} from 'react-native';

const Inputs = StyleSheet.create({
  inputarea: {
    borderWidth: 1,
    width: 300,
    color: 'black',
    padding: 15,
    marginTop: 15,
  },
});

export default Inputs;
